services:
  postgres:
    image: postgres:15-alpine
    container_name: nutridash_postgres
    environment:
      POSTGRES_USER: nutridash_user
      POSTGRES_PASSWORD: nutridash_pass
      POSTGRES_DB: nutridash_dev
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nutridash_user -d nutridash_dev"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: nutridash_redis
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
